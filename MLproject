name: Recommender_System

entry_points:
  train-pipeline:
    command: "python pipelines/train.py"
  predict-pipeline:
    command: "python pipelines/predict.py"
  eval-pipeline:
    parameters:
      version: {type: str, default: "v1"}
    command: "python pipelines/eval.py --version {version}"
